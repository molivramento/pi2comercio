server {
    listen 80 localhost;
    listen [::]:80 localhost;
    server_name localhost;

    location / {
        proxy_pass http://fastapi:8000;
    }

    location /static/ {
        alias /app/static;
}